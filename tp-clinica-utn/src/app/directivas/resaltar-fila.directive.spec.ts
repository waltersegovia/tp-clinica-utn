import { ResaltarFilaDirective } from './resaltar-fila.directive';

describe('ResaltarFilaDirective', () => {
  it('should create an instance', () => {
    const directive = new ResaltarFilaDirective();
    expect(directive).toBeTruthy();
  });
});
