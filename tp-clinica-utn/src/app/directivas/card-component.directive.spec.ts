import { CardComponentDirective } from './card-component.directive';

describe('CardComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new CardComponentDirective();
    expect(directive).toBeTruthy();
  });
});
