import { EspecPacientePipe } from './espec-paciente.pipe';

describe('EspecPacientePipe', () => {
  it('create an instance', () => {
    const pipe = new EspecPacientePipe();
    expect(pipe).toBeTruthy();
  });
});
