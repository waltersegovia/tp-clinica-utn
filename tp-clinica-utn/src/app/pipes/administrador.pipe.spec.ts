import { AdministradorPipe } from './administrador.pipe';

describe('AdministradorPipe', () => {
  it('create an instance', () => {
    const pipe = new AdministradorPipe();
    expect(pipe).toBeTruthy();
  });
});
