import { FormatKeyValuePipe } from './format-key-value.pipe';

describe('FormatKeyValuePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatKeyValuePipe();
    expect(pipe).toBeTruthy();
  });
});
